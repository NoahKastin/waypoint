---
description: Mark a Waypoint quest complete by id or unique prefix. Use when the user types /quest-done <id> or says they finished a quest.
argument-hint: <quest-id>
allowed-tools: Bash(waypoint quest done *)
---

Run `waypoint quest done $ARGUMENTS` and show the output verbatim. The CLI handles XP awarding, Rep updates, and flavor text — don't add your own.
